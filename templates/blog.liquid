<h1>{{ blog.title }}</h1>

{% for article in blog.articles %}
  <a href="{{ article.url }}">
    <img src="{{ article.image | image_url }}" width="250" height="auto">
    <h2>{{ article.title }}</h2>
    <p>{{ article.content }}</p>
    <p>{{ article.author }}</p>
  </a>
{% endfor %}
